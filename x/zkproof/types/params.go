package types

const (
    // ModuleName defines the module name
    ModuleName = "zkproof"

    // StoreKey is the store key string for zkproof
    StoreKey = ModuleName

    // RouterKey is the message route for zkproof
    RouterKey = ModuleName

    // QuerierRoute defines the module's query routing key
    QuerierRoute = ModuleName
)
